#!/usr/bin/env zsh

pip install --upgrade --user \
    git+git://github.com/powerline/powerline \
    aws-shell \
    tvoverlord \
    yip \
    yamllint \
    subliminal \
    pipenv \
    azure-cli \
    kube-shell

pip2 install --upgrade --user \
     bugwarrior \
     keyring \
     dbus-python
